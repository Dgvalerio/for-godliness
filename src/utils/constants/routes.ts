interface Routes {
  home: string;
  signIn: string;
  addMember: string;
  addChurch: string;
}

export const routes: Routes = {
  home: '/',
  signIn: '/sign-in',
  addMember: '/sign-in',
  addChurch: '/sign-in',
};
