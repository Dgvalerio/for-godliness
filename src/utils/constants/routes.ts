interface Routes {
  home: string;
  signIn: string;
  addRecord: string;
  addChurch: string;
}

export const routes: Routes = {
  home: '/',
  signIn: '/sign-in',
  addRecord: '/sign-in',
  addChurch: '/sign-in',
};
